# Slice 24 — Container packaging and schema readiness

Implementation 2026-09-19, no new migration. Added an explicit build-context allowlist, separate unprivileged web/worker images, a bounded Compose stack and release/restore instructions. Compiled workspace packages expose only their runtime distribution in production deployments. Web readiness verifies required migration checksums with a bounded database query, returns a minimal uncached result and reports unavailable configuration/storage as HTTP 503. Worker startup requires compatible schema and caps application/queue connection pools.

Host formatting, typed lint, strict checks, production build and unit tests pass; persistence tests verify readiness against the applied migration set and a changed checksum. Compose configuration validates. Both Linux images built successfully. The isolated container proof passed non-root/read-only operation, migration-required startup, minimal 503/200 readiness, changed-checksum detection, an Arabic page and PNG asset, durable worker processing and graceful shutdown. All 81 deployed worker package versions were present in the source lockfile; proof containers, network and temporary credentials were removed. Run `pnpm test:containers` against the built images to repeat the check. See [container deployment](container-deployment.md); no deployment, credentials, DNS, provider access or off-host backup is implied.
