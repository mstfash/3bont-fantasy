# ADR 0030 — Evidence-backed provider identities

Accepted 2026-09-19. Bind each local season to an explicit provider league/year and map clubs, footballers and fixtures from successful stored responses instead of matching names. Keep every mapping revision when correcting or retiring a link, require fixture club consistency, and preserve the exact version used by subsequent imports so later identity repairs cannot silently rewrite sporting history.
